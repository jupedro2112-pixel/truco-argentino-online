// Toggle this to use custom images dropped into /public/cards/.
// File pattern expected: {suit}-{number}.{CARD_IMAGE_EXT}, e.g. "oro-1.svg".
// If a file is missing, the renderer falls back to built-in SVG art.

export const USE_IMAGE_DECK = false
export const CARD_IMAGE_EXT = 'svg'
export const CARD_BACK_IMAGE = null  // e.g. '/cards/back.svg'
